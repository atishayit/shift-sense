version: "3.9"
services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: shiftsense
      POSTGRES_PASSWORD: shiftsense
      POSTGRES_DB: shiftsense
    ports: ["5432:5432"]
    volumes: [dbdata:/var/lib/postgresql/data]
volumes:
  dbdata:
